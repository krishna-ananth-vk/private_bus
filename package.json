{
  "name": "private_bus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "meta_bg": "node scripts/capture-preview.js",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^8.2.0"
  }
}
