{
  "name": "private_bus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "meta_bg": "sips -Z 1200 -s format jpeg -s formatOptions 70 public/meta-bg.png --out public/preview.jpg"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
